# datasources-benchmark

- To build container with no cache
docker-compose build --no-cache

- To start the container:
docker-compose up  (-d if no logs)

- To start the containers in interactive mode:
docker-compose run --rm app bash
